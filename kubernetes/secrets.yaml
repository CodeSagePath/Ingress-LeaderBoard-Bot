---
apiVersion: v1
kind: Secret
metadata:
  name: bot-secrets
  namespace: ingress-leaderboard
  labels:
    app: ingress-leaderboard-bot
    component: secrets
type: Opaque
data:
  # Base64 encoded values - replace with actual encoded values
  # telegram-bot-token: echo -n "your_telegram_bot_token" | base64
  telegram-bot-token: ""
  # db-password: echo -n "your_database_password" | base64
  db-password: ""
  # secret-key: echo -n "your_secret_key" | base64
  secret-key: ""
  # encryption-key: openssl rand -base64 32
  encryption-key: ""
  # redis-password: echo -n "your_redis_password" | base64
  redis-password: ""
---
apiVersion: v1
kind: Secret
metadata:
  name: bot-secrets
  namespace: ingress-leaderboard-staging
  labels:
    app: ingress-leaderboard-bot
    component: secrets
type: Opaque
data:
  # Base64 encoded values for staging
  telegram-bot-token: ""
  db-password: ""
  secret-key: ""
  encryption-key: ""
  redis-password: ""